#include "main.h"
#include <stdio.h>

/**
 * main - a program that prints its name, followed by a new line
 * @argc: integer type of argument
 * @argv: an array of character pointers
 * Return: 0
 */
int main(int argc_attribute_((unused)); char *argv[])
{
	{
	printf("%s\n", *argv[]);
	}
	return (0);
}
